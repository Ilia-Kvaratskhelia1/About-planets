import ReactDOM from 'react-dom/client'
import "./Main.css"
import Routing from './Components/Planets/Routing/Routing'

ReactDOM.createRoot(document.getElementById('root') as HTMLElement).render(
<Routing/>
)
